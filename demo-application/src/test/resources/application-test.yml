accounts.repo.init-capacity: 5

spring:
  datasource:
    url: "jdbc:derby:memory:dbo;create=true"
    username: app
  jpa:
    hibernate.ddl-auto: update
    generate-ddl: true
    show-sql: true
    properties.hibernate.dialect: org.hibernate.dialect.DerbyDialect
